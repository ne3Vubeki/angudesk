var server = require("./server"),
	router = require("./router"),
	pages = require("./requestHandlers").pages;

server.start(router.route, {
    "/": pages.start,
    "/*": pages.start,
    "/*.*": pages.link
});